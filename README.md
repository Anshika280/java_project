This project implements Dijkstra’s Algorithm in Java to find the shortest path from a source vertex to all other vertices in a weighted graph.
The algorithm works on graphs with non-negative edge weights and is widely used in network routing and map navigation systems.
